# Document Workflow

```text
scan / photo
  -> image cleanup
  -> OCR
  -> text normalization
  -> field extraction
  -> validation
  -> searchable record
```

The first artifact focuses on the post-OCR stage: turning noisy text into a small structured JSON summary.
